// Utility functions to parse features .csv and rule order .txt files

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

// Parses features .csv file from in memory
// Returns 1 on success, 0 on failure (and prints to stderr)
char parse_csv(uint8_t *csv, uint64_t csv_len) {
    return 1;
}

// Parses rules file from FILE *
// Returns 1 on success, 0 on failure (and prints to stderr)
char parse_rules(FILE *rules_fp) {
    return 1;
}
